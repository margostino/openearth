scalar Date

type DatasetSource {
  url: String!
  name: String!
}

type Dataset {
  id: ID!
  name: String!
  description: String!
  category: String!
  source: DatasetSource!
  url: String!
  last_updated: Date!
}

type Query {
  datasets(id: ID, name: String, category: String): [Dataset!]!
}